# 0.2.10 — 通知电压与单次里程

- 按用户确认的两行样式，标题保留 `S9 电量 · 80%`，正文改为 `38.10 V · 2.3 km`。通知固定使用伏和千米。
- 使用本次认证会话 DP 20 与 DP 5，按车型定义缩放；未知显示破折号，零值正常显示，不使用历史读数。电压或里程独立变化也会触发刷新。
- 保留原有后台蓝牙服务、断开连接按钮和断连移除通知。Java 编译、APK 签名校验通过；iQOO 通知实际外观待安装确认。

# 0.2.9 — 底部调节卡片

- 按用户澄清，设置页恢复三档限速和自动关机四行入口；点击单项在底部弹出小卡片，再通过横向滑条及减号/加号调节。
- 保留松手发送、接收确认、范围步长和错误回退。列表仅显示回报值，底部卡片区分调节目标和待确认状态；关闭、返回或遮罩取消未提交草稿并恢复焦点。
- 保留首页电流、功率及其他既有功能。30 项桌面界面检查、APK 编译与签名校验通过；实机交互仍待复测。

# 0.2.8 — 滑条微调与首页电流功率

- 三档限速、自动关机改为直接展示的横向滑条，左减右加；范围和步长沿用车型物模型。自动关机 5–90 分钟、每步 5 分钟。
- 拖动仅显示待发送目标，松手发送一次；遥测刷新保留拖动值，取消或断连不发送。待确认阻止重复操作，匹配回报完成，拒绝/超时恢复回报值，不伪造设置成功。
- 首页增加当前电流与当前功率卡片，设置页保留，缺失为破折号。核对 DP 21 scale=2 单位 A、DP 22 scale=0 单位 W；现有实车诊断两项为零，动态准确度未验证。
- 29 项桌面 UI 检查通过，包括滑条提交时机、加减边界、拒绝/超时、取消、断连、公里/英里、非零电流功率和小屏布局。APK 编译与签名校验通过，实车交互待复测。

# 0.2.7 — 单击控制、开锁动画与车辆信息

- 同一条 SVG 锁梁沿左侧连续转开，锁身保留；随真实锁车回报变化，支持反向闭合与系统减少动态效果设置。
- 前灯单击交替发送已验证的 DP 8 布尔指令。首次开灯，收到成功 ACK 后才按车辆持久化方向，失败或超时不改变方向；高亮仅代表最后接受的开灯指令，不伪造灯光回报。其他 App 或车身操作可能导致方向不同步。
- 零启动/非零启动按 DP 16 回报单击切换，保留未知状态、待确认和车速限制。
- 设置页直接列出三档限速，各自按状态启用，点击直接调整数值；放宽设置页间距并允许滚动。
- 首页增加电压与当前温度小卡片，设置页也保留这两项；DP 18 按物模型 scale=1 显示，不称为电池温度。无数据显示破折号。
- 重画首页滑板车、导航小图标与 Android 应用图标。
- 27 项桌面界面检查通过，涵盖动画中间帧、双向单击、失败重试方向、温度缩放、独立限速和小屏布局；APK 编译与签名校验通过。Android 实机动画及前灯交替控制仍待车主复测。

# 0.2.6 — 恢复有效的前灯开关指令

- 车主确认 0.2.3 的明确“开启／关闭”选项可以控制前灯。撤销 0.2.4 根据原面板回弹行为作出的固定 false 触发推断。
- 保留紧凑前灯图标，点击后选择开启（DP 8=true）或关闭（DP 8=false）；原生层恢复允许两个布尔值。
- 前灯协议 ACK 只提示对应指令已接收，不把它当作实际灯光状态，不等待不存在的 DP 8 状态回报。
- 保留历史电量和里程、四种骑行模式、锁车状态修正与后台普通通知。
- 24 项桌面 UI 检查通过，覆盖前灯开启/关闭两个值及取消不发送；APK 编译、签名和打包内容检查通过。新版仍需车主实车复测。

# 0.2.5 — 紧凑首页、历史读数与四种模式

- 通过电脑手机投屏查看原布局，压缩电量区及留白。锁车和前灯卡片 65px 高、40px 图标，隐藏卡片内文字但保留无障碍标签。
- 记住本车最后回报的电量与总里程，离线或等待读取时标注“上次记录”；仅保存真实认证后的 DP，不保存预览值，不以缓存启用控制。首次升级后需成功读取一次才能建立记录。
- 骑行模式补齐行人/经济/日常/运动（DP 15=0/1/2/3）；三档限速仍对应经济/日常/运动。
- 用户确认通用实时通知未进入原子岛。核实专用接入要求后，按用户要求移除实时通知提升请求及权限；保留 connectedDevice 服务和普通电量通知。
- 24 项桌面 UI 检查通过，包括缓存与实时零电量、四模式、三档限速和 360×720 / 393×780 / 412×820 下完整显示控制。Android 首次缓存持久化及实际排版仍需安装验证。

# 0.2.4 — 前灯触发、锁车含义与后台电量通知

- 根据车主实测，DP 1=true 表示已解锁；修正首页和通知的状态显示，锁车发送 false、解锁发送 true。
- 根据原面板开关回弹行为，将前灯改为单次 DP 8=false 触发，不显示未经确认的开/关状态。收到协议 ACK 后提示“已接收切灯指令”，不将其等同于真实灯光确认；脉冲实际效果仍待车主复测。
- BLE 由 Application 持有，独立 connectedDevice 前台服务保持连接；Activity 停止或销毁不再关闭 GATT。前台自动扫描保留，退出预览可恢复；清除任务或点击通知“断开连接”结束会话。
- 新增通知权限请求和设置入口；通知显示车辆电量及锁车状态，断连移除通知。无网络和定位权限。
- Android 36 以上通过运行时可用的公开 API 请求实时通知与电量短文本；不保证 OriginOS 原子岛接受，未申请 vivo 专用接入资格，不伪装其他应用或场景。
- 21 项桌面 UI 检查、编译及签名校验通过。无 Android 设备连接，未完成服务生命周期、iQOO 13 后台存活、原子岛及切灯指令实测。

# 0.2.3 — 控制可用性与前台自动连接

- 实车诊断确认认证成功，车速 DP 2 为零，前灯 DP 8 未回报。移除速度状态的 12 秒失效规则，保留本次连接最后状态、运动/未知速度拦截和断连清空。
- 前灯已认证但缺少状态时允许选择明确开/关指令，不把未知状态伪装成关闭，也不提前显示设置成功。
- 命令发送、回报成功、拒绝及超时有可见提示，独立保存最近 40 条控制事件，避免高频遥测冲掉操作日志。
- 已配置、已授权、蓝牙开启时，每次打开/回到 App 自动扫描配置中的 MAC 并认证；前台扫描 12 秒，后台停止，预览暂停，不连接其它设备。
- 20 项桌面 UI 检查通过，包含用户缺少 DP 8 的状态复现、明确开/关指令、结果提示及预览抑制信号。协议层未修改，沿用上一版 32 项协议检查。自动扫描/生命周期、实际控制仍需 Android 真机复测。

# 0.2.2 — 修复已确认的 Invalid padding

- 0.2.1 真机日志明确显示：177 字节加密包完整重组（transportFill=0），长度与 CRC 通过后，在全零填充检查失败。不是本次传输层末包溢出。
- 按 SDK 的长度与 CRC 边界读取消息，不要求 CRC 后的 AES 对齐区必须全零；仍限制该区域为 0–15 字节，不接纳额外加密块，不跳过 CRC 或车辆身份校验。
- 使用独立 Node/OpenSSL 合成向量在修改前复现 Invalid padding，修改后验证零、PKCS 风格及其它非零尾部只返回声明的载荷；错误 CRC 和额外块继续拒绝。
- 32 项 JVM 协议检查、APK 构建与签名校验通过。完整握手及车辆控制仍待实车验证，覆盖安装保留原配置。

# 0.2.1 — 认证回复解析兼容与诊断

- 用户提供的 0.2.0 真机诊断确认：已完成 GATT 连接和通知订阅，发送两片设备信息请求、收到十次通知，随后在收到可解析帧之前失败；日志仅有 IllegalArgumentException，无法唯一确定原因。
- 对齐 SDK 分包接收方式：固定 20 字节末包按声明总长度截取，忽略传输层尾部填充；仍校验加密包长度、CRC 和加密层补零。非固定长度溢出继续拒绝。
- 同一未完成帧中完全相同的上一片通知不再重复拼接。
- 诊断增加固定错误原因、解析阶段、分片序号及长度计数，不保存密钥、IV、密文或解密载荷。
- 27 项 JVM 协议检查通过，APK 编译和签名校验通过；此兼容性修改是否解决本次实车故障尚待验证。无需重新登录或重新导入已保存配置。

# 0.2.0 — 本地认证与车辆控制（待实车验证）

- 获取车辆本地凭据和官方物模型；一次性文件导入、Android Keystore 加密保存，APK 不含车辆密钥。
- FD50 通知、V4 握手、AES 封包、状态查询和 DP 控制；核对目标身份，不激活未绑定设备。
- 按回报启用和确认功能，加入静止状态限制、超时、断连清理。
- 21 项 JVM 协议检查、17 项桌面界面检查通过；Android 运行与实车仍待验证。

# 0.1.2 — 修正车辆连接状态的误报

- 服务发现成功后进入 authentication_unavailable，明确表示认证尚未实现，不再显示绿色“蓝牙已连接”。这不是认证尝试失败，而是该流程尚未接入。
- 分开报告底层链路、车辆认证和控制就绪状态。诊断页面支持此状态下导出，断连后保留上次设备服务供分析。
- 本次没有接通车辆认证或控制，仍需用户车辆的实际服务信息与适用的认证方案。不能把状态修正当作蓝牙功能已经修好。
- 保持包名与签名，versionCode 3。

# 0.1.1 — 修复启动闪退

- 修复 MainActivity 在 setContentView 之前调用 Window.getInsetsController 的错误。此时 PhoneWindow.mDecor 尚未初始化，系统实现会解引用空对象。
- 状态栏外观设置改为在内容视图挂载后执行，通过 View.getWindowInsetsController 取得控制器，并检查空值及 Activity 销毁状态。
- 保持应用包名及原签名，将 versionCode 升至 2，可覆盖安装 0.1.0。
- 增加独立的 Android instrumentation 启动测试，覆盖首次启动、离线 WebView 加载、原生桥、预览交互、返回流程及重启后本地设置。

依据：[Android PhoneWindow 实现](https://android.googlesource.com/platform/frameworks/base/+/refs/tags/android-15.0.0_r5/core/java/com/android/internal/policy/PhoneWindow.java)。getInsetsController 直接调用 mDecor.getWindowInsetsController；旧代码在创建内容视图前访问它。

验证：0.1.1 编译及签名校验通过，11 项桌面界面回归通过，Android instrumentation 测试 APK 编译通过。电脑没有连接安卓设备；模拟器及系统镜像下载多次超时，未完成 Android instrumentation 执行。没有取得用户手机的崩溃日志，因此修复的是已确认存在、能够导致启动闪退的代码缺陷，不能声称已经在用户的 OriginOS 6 手机上验证恢复正常。

0.1.2 验证：11 项桌面界面检查通过，包含未认证链路不能显示连接成功、断连后诊断与导出状态；APK 编译和签名校验通过。未完成安卓运行与车辆认证实测。
